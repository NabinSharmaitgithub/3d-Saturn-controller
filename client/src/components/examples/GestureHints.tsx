import GestureHints from '../GestureHints';

export default function GestureHintsExample() {
  return (
    <div className="w-full h-screen bg-black relative">
      <GestureHints isVisible={true} />
    </div>
  );
}
